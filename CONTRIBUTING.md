# Contributing

Every change requires: governing requirement IDs; canonical-owner impact; privacy/security analysis; migration/rollback impact; tests; review → fix → fresh review → fix → retest; deterministic package parity; and truthful completion labels. Do not commit generated `dist/` files, secrets, provider credentials or restricted evidence. Runtime must remain disabled by default.
